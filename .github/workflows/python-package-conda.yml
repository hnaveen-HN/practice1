name: Multi Job Workflow

on: workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step in Job1
        run: echo "This is Job 1 running"

  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Step in Job2
        run: echo "This is Job 2 running"
